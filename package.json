{
  "name": "vita",
  "private": true,
  "workspaces": [
    "backend/*",
    "ai-engine"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:migrate": "cd database && node migrate.js",
    "db:seed": "cd database && node seed.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
